const structureReducer = (state: any, action: any) => {
    switch (action.type) {
        case 'ADD':
            return state;
        case 'REMOVE':
            return state;
    }
}

export default structureReducer;