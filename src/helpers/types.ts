export type formInputType = {
    appname: string,
    typescript: boolean,
    prettier: boolean,
    tailwind: boolean,
    bootstrap: boolean,
    styledcomponents: boolean,
    normalize: boolean,
    reactrouter: boolean,
    proptypes: boolean
}