export type formInputType = {
    appname: string,
    typescript: boolean,
    tailwind: boolean,
    bootstrap: boolean,
    styledcomponents: boolean,
    normalize: boolean,
    reactrouter: boolean,
    propstype: boolean
}