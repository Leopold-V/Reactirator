import React from 'react'

export const Header = () => {
    return (
        <header className="py-8 flex flex-col items-center">
            <h1 className="pb-4 font-bold text-4xl text-center text-white">Create-react-app generator</h1>
            <p className="text-center">Generate a cra project with your favorite config embedded without touching a shell, everything is automatized so gain time without bothering about config, enjoy.</p>
        </header>
    )
}
